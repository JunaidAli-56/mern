import { createAction } from "@reduxjs/toolkit";

// const example = createAction(type)...
export const removeAllUsers = createAction('removeAllUsers')


// it totally independent  u can use anywhere in extraReducer..